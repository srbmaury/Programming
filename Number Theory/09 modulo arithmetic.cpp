// a is congruent to b under modulo N if the leave 
// same remainder when divided by N a ≡ b(mod N)
/*
if
	a≡b(mod N)
then
	a-b ≡ 0 (mod N)
	a-b is divisible by N


if
	a*b=c
then
	a(mod N) * b(mod N) ≡ c (mod N)
	a%N * b%N ≡ c%N


if
	a≡b(mod N)
then
	a^k ≡ b^k (mod N)
*/
