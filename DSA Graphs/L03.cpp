// Connected components

// Strongly connected components (in directed graphs)
// Assuming graph is not directed we count weakly connected components
/*


int main(){
	int cc_count = 0;
	for(int i=1; i<=n; i++)
		if(vis[i]==0)
			dfs(i), cc_count++;

	cout<<"Number of connected components "<< cc_count;
}

*/
